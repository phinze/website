server {
    listen 80;
    server_name phinze.com;
    return 301 https://inze.ph$request_uri;
}

server {
    listen 80;
    server_name inze.ph;
    root /usr/share/nginx/html;
    index index.html;

    # Strip trailing punctuation dragged in from pasted markdown/prose links
    if ($request_uri ~ "^(.+)[\)\],;.!?]+$") {
        return 301 $1;
    }

    location / {
        try_files $uri $uri/ $uri.html =404;
    }

    error_page 404 /404.html;
}
